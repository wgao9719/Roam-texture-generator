# Configure npm to install packages for Linux environment
node-linker=hoisted
# Force Sharp to install for Linux x64
platform=linux
arch=x64
libc=glibc
# Make sure to include optional dependencies
include=optional 